const greeting = "Hi";

function exec() {
    const greeting = "Hello";
    return greeting;
}

// Objects are groups an unordered collection of related data
const client = {
    name: "Pedro",
    age: 32,
    weight: 90,
    address: {
        street: "Street One",
        number: 911
    }
}

console.log(greeting);
console.log(exec());
console.log(client);